import 'package:flutter/material.dart';

class PersonReports extends StatelessWidget {
  const PersonReports({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("reports"),
    );
  }
}
